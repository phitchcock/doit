<%= form_for(@list) do |f| %>
  <%= render 'shared/errors', obj: @list %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <%= f.submit 'btn btn-success' %>
  </div>
<% end %>